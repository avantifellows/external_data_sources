# IISER closing-ranks pipeline dependencies.
# PDF text extraction uses poppler's pdftotext (system binary: brew install poppler).
pandas>=2.0
pyarrow>=14.0
google-cloud-storage>=2.10
google-cloud-bigquery>=3.11
